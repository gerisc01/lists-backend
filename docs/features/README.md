# Features

There are no feature pages in this repo. Every feature spanning both repos is documented **once**,
in [`games-lists/docs/features/`](../../../games-lists/docs/features/), and its `## Files` manifest
names backend paths with `be` in the repo column.

[`BY-FILE.md`](BY-FILE.md) is the generated inverse for this repo — which feature owns each `src/`
file here. Never hand-edit it:

```
node ../../../games-lists/scripts/check-docs.js
```
